[package]
name = "transfer-session"
version = "0.1.0"
edition = "2021"

[dependencies]
casper-contract = { path = "../patches/casper-contract" }
casper-types = "6.0.1"

[lib]
crate-type = ["cdylib"]

[workspace]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
