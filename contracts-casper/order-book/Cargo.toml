[package]
name = "order-book"
version = "0.1.0"
edition = "2021"

[dependencies]
casper-contract = { workspace = true }
casper-types = { workspace = true }

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
