{
  "name": "chimera-inference-api",
  "version": "0.1.0",
  "description": "Lightweight HTTP API for Chimera inference network stats and referrals",
  "type": "module",
  "scripts": {
    "dev": "tsx server.ts",
    "start": "node --import=tsx server.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "node-seal": "^7.0.0"
  },
  "devDependencies": {
    "tsx": "^4.11.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
